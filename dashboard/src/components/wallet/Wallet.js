import React from "react";

function Wallet() {

    const headingStyle = {
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100vh",
        margin: 0,
        textAlign: "center",
      };

      return <h1 style={headingStyle}>Rihan Wallet Page</h1>;
}

export default Wallet;