import React from "react";

function Supporters() {

    const headingStyle = {
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100vh",
        margin: 0,
        textAlign: "center",
      };

      return <h1 style={headingStyle}>Rihan Supporters Page</h1>;
}

export default Supporters;